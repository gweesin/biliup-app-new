# 2026-09-11

## ai.rs：图像识别 temperature 改为 1.0（Kimi 兼容）
- `ai_analyze_video` 调用 `request_chat(&ai.vision, messages, 0.2, ...)` 改为 `temperature = 1.0`，同步更新注释。
- 原因：Kimi（moonshot 系）视觉模型要求 temperature 固定为 1，传其它值会报参数错误。
- `generate_ai_title` 仍为 1.2（纯文本请求，不受此限制）。
- 已更新 MEMORY.md 中对应的架构说明。

## submit 接口合并后处理（删除源文件 + 加入合集）
- 背景：原来前端在 submit 成功后按顺序串行调用 delete_file / get_video_season / get_video_cid / switch_season。
- `src-tauri/src/commands/utils.rs`：将 delete_file / get_video_season / get_video_cid / switch_season 的核心逻辑抽为 pub 函数
  `delete_local_video_file`、`query_video_season`、`query_video_cid`（带 3 次重试）、`switch_season_inner`，Tauri 命令保留为薄包装。
- `src-tauri/src/commands/upload.rs`：`submit` 命令投稿/编辑成功后内部完成 ①删除 form.videos 的 original_file_path 源文件（失败仅日志）
  ②合集处理（新增未配置 season 跳过；查 old_season_id、解析 cid、不一致才切换），返回 `SubmitOutcome { data, deleted_file_paths, season }`。
  data 为显式字段（非 serde flatten），避免返回体 data 缺失时序列化失败。合集/删除失败不影响投稿成功。
- 前端 `src/composables/useSeparateSubmit.ts`：删除 `syncSeasonAfterSubmit`/`resolveSeasonCid`，新增 `handleSubmitOutcome(resp, template, videosRef)`
  消费返回结果（清空已删除视频的 original_file_path + 合集成功/失败提示）；resp 的 aid/bvid 读取改为 `resp.data.*`。
- `src/utils/videoFileCleanup.ts`：新增 `clearDeletedVideoFilePaths(videos, deletedPaths)`。
- 仍保留：前端 500ms 等待、handleAutoEditAfterSubmit（创建编辑模板/刷新列表，属前端页面逻辑）。
- 注意：`deleteOriginalVideoFile` 仍被 useVideoImport.ts 使用（导入场景），命令与工具函数保留。
