# 2026-08-23 工作日志

## 实现"封面匹配路径"配置功能
在 biliup-app-new 中实现了封面匹配路径配置和按标题关键字自动匹配封面功能。

### 修改内容
1. **Rust 端**
   - `src-tauri/src/models/user_config.rs`：`ConfigRoot` 新增 `cover_match_path: String` 字段（`#[serde(default)]`），更新 `save_global_config` 方法签名和 `Default`。
   - `src-tauri/src/commands/config.rs`：`save_global_config` 命令新增 `cover_match_path` 参数。
   - `src-tauri/src/commands/utils.rs`：新增 `list_cover_images(dir_path, keywords)` 命令，返回 `CoverImageItem { name, path, data_url }`（base64 预览，递归最多 5 层，图片扩展名过滤，>8MB 跳过）。
   - `src-tauri/src/lib.rs`：注册 `list_cover_images` 命令。

2. **前端**
   - `src/stores/user_config.ts`：`ConfigRoot` 接口新增 `cover_match_path`，`updateGlobalConfig` 传 `coverMatchPath`。
   - `src/components/GlobalConfig.vue`：新增"封面匹配路径"配置项（el-input + FolderOpened 按钮选择文件夹，`open({ directory: true })`），含保存/加载/重置/未保存检查。
   - `src/views/MainView.vue`：新增 `COVER_MATCH_KEYWORDS`（三国英雄名约170个）、`refreshCoverMatch`（watch 标题、300ms 防抖）、`setCoverFromMatch`（点击上传设置封面）、匹配封面展示区（封面 el-form-item 内上传器下方）。

### 验证
- `cargo check` 通过。
- `vue-tsc --noEmit` 通过。
- prettier/cargo fmt 已格式化。

## 封面匹配图片 hover 放大（2026-08-23）
- `MainView.vue` 中 `.cover-match-item:hover` 由 `scale(1.05)` 改为 `scale(2.5) translate(15px, 8px)`，参照 `.cover-uploader .cover-image:hover`（scale(3)）效果，添加 box-shadow 与 z-index:999，`.cover-match-item` 增加 `position: relative; z-index: 1`。

## 封面匹配关键字抽离为 JSON（2026-08-23）
- 新建 `src/constants/cover-match-keywords.json`，存放三国英雄名称关键字数组（约146个）。
- `MainView.vue` 删除内联 `COVER_MATCH_KEYWORDS` 常量，改为 `import COVER_MATCH_KEYWORDS from '../constants/cover-match-keywords.json'`（tsconfig 已开启 `resolveJsonModule`，`vue-tsc` 通过）。
- 后续维护关键字只需编辑该 JSON 文件。
